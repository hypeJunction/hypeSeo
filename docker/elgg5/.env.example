# Per-plugin docker test environment for Elgg 5.x.
# Copy this file to docker/elgg5/.env and fill in PLUGIN_ID.

# Lowercase Elgg plugin id. Must match the plugin directory name.
PLUGIN_ID=

# Optional: override host-side ports if the defaults clash with another stack.
# ELGG_PORT=9583
# DB_PORT=10583
